import CustomItems from "./CustomItems";
import ScheduleTitle from "./ScheduleTitle";

const MakeCustomItem = () => {
  return (
    <>
      <ScheduleTitle title="나만의 아이템 만들기" />
      <CustomItems />
    </>
  );
};

export default MakeCustomItem;
