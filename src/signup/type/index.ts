export interface Agreements {
  ageAgreement: { essential: boolean; checked: boolean };
  catcherAgreement: { essential: boolean; checked: boolean };
  marketingAgreement: { essential: boolean; checked: boolean };
  privacyAgreement: { essential: boolean; checked: boolean };
  locationAgreement: { essential: boolean; checked: boolean };
}
